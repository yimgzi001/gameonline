# 🤝 贡献指南

感谢你考虑为游戏在线项目做贡献！

## 📝 如何贡献

### 报告问题

如果你发现了 bug 或有功能建议：

1. 先搜索 [Issues](https://github.com/yourusername/gameonline/issues) 确保问题未被报告
2. 创建新的 Issue，使用清晰的标题和描述
3. 如果是 bug，请提供：
   - 复现步骤
   - 预期行为
   - 实际行为
   - 截图（如果适用）
   - 环境信息（浏览器、操作系统等）

### 提交代码

1. **Fork 项目**

2. **创建分支**
   ```bash
   git checkout -b feature/your-feature-name
   # 或
   git checkout -b fix/your-bug-fix
   ```

3. **进行修改**
   - 遵循代码风格（使用 ESLint 和 Prettier）
   - 添加必要的注释
   - 确保代码通过类型检查

4. **测试修改**
   ```bash
   npm run lint        # 检查代码风格
   npm run type-check  # 检查类型
   npm run build       # 确保构建成功
   ```

5. **提交改动**
   ```bash
   git add .
   git commit -m "feat: add new game category"
   ```

   提交信息格式：
   - `feat:` 新功能
   - `fix:` 修复 bug
   - `docs:` 文档更新
   - `style:` 代码格式（不影响功能）
   - `refactor:` 重构
   - `test:` 添加测试
   - `chore:` 构建过程或辅助工具的变动

6. **推送到 GitHub**
   ```bash
   git push origin feature/your-feature-name
   ```

7. **创建 Pull Request**
   - 使用清晰的标题描述你的修改
   - 在描述中详细说明改动内容
   - 链接相关的 Issue（如果有）

## 🎮 添加新游戏

添加游戏的步骤：

1. 准备游戏资源：
   - 游戏 URL（iframe 可嵌入）
   - 缩略图（800x500px，< 200KB）
   - 游戏信息（标题、描述、控制说明等）

2. 将缩略图放到 `public/images/games/`

3. 在 `src/data/games.ts` 添加游戏数据

4. 测试游戏页面：
   ```bash
   npm run dev
   # 访问 http://localhost:3000/games/your-game-slug
   ```

5. 提交 PR

## 📋 代码规范

### TypeScript

- 使用类型注解，避免 `any`
- 使用接口（interface）定义数据结构
- 导出类型供其他文件使用

### React

- 使用函数组件和 Hooks
- 组件名使用 PascalCase
- 文件名与组件名匹配

### 样式

- 优先使用 Tailwind CSS 类
- 自定义样式写在 `globals.css`
- 使用语义化类名

### 命名约定

- 文件名：kebab-case（如 `game-card.tsx`）
- 组件名：PascalCase（如 `GameCard`）
- 变量/函数：camelCase（如 `getGameBySlug`）
- 常量：UPPER_SNAKE_CASE（如 `MAX_GAMES`）

## 🧪 测试

目前项目还没有测试套件，欢迎贡献！

可以添加：
- 单元测试（Jest）
- 组件测试（React Testing Library）
- E2E 测试（Playwright）

## 📚 文档

文档改进也是重要的贡献：

- 修复文档错误
- 添加使用示例
- 翻译文档
- 改进代码注释

## 🎨 设计贡献

如果你擅长设计：

- 改进 UI/UX
- 设计游戏缩略图模板
- 创建 Logo 和图标
- 优化移动端体验

## ❓ 需要帮助？

- 加入我们的 Discord 社区
- 查看 [SETUP.md](./SETUP.md) 了解开发环境配置
- 在 Issue 中提问

## 📜 许可证

通过提交代码，你同意你的贡献将在 MIT 许可证下发布。

---

再次感谢你的贡献！🙏

